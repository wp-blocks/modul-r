import { describe, expect, test } from '@jest/globals';

/* JEST testing */
describe( 'vsge-model-viewer test', () => {
	it( 'it can make simple additions', () => {
		expect( 1 + 2 ).toBe( 3 );
	} );
} );
